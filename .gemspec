Gem::Specification.new do |s|
  s.name        = 'json-next'
  s.version     = JSON::Next::VERSION
  s.licenses    = ['Public Domain']
  s.summary     = "json-next - read generation y / next generation json versions (HanSON, SON, JSONX/JSON11, etc.) with comments, unquoted keys, multi-line strings, trailing commas, optional commas, and more"
  s.description = s.summary
  s.authors     = ["Gerald Bauer"]
  s.email       = 'ruby-talk@ruby-lang.org'
  s.homepage    = 'https://rubygems.org/gems/example'
  s.metadata    = { "source_code_uri" => "https://github.com/example/example" }
end


# Hoe.spec 'json-next' do

#   self.version = JSON::Next::VERSION

#   self.summary = 'json-next - read generation y / next generation json versions (HanSON, SON, JSONX/JSON11, etc.) with comments, unquoted keys, multi-line strings, trailing commas, optional commas, and more'
#   self.description = summary

#   self.urls    = ['https://github.com/json-next/json-next']

#   self.author  = 'Gerald Bauer'
#   self.email   = 'ruby-talk@ruby-lang.org'

#   # switch extension to .markdown for gihub formatting
#   self.readme_file  = 'README.md'
#   self.history_file = 'HISTORY.md'

#   self.licenses = ['Public Domain']

#   self.spec_extras = {
#    required_ruby_version: '>= 1.9.2'
#   }

# end
